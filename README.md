# Concierge MCP

Private Cloudflare-hosted MCP server for personal use.

Endpoint: `https://concierge.j1.io/mcp`

## Tool

- `read_webpage_as_markdown`: renders a public HTTP(S) URL with Cloudflare Browser Run, waits for `networkidle0`, and returns the page as Markdown.
