{
  "name": "concierge",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --var CONCIERGE_DEBUG:true",
    "start": "wrangler dev --var CONCIERGE_DEBUG:true",
    "cf-typegen": "wrangler types",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "^0.8.1",
    "agents": "^0.17.1",
    "just-pick": "^4.2.0",
    "octokit": "^5.0.5",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "6.0.3",
    "wrangler": "^4.110.0"
  }
}
